{
  "name": "@bot-html/addin",
  "version": "1.0.0",
  "description": "Office Web Add-in манифест и хостинг для Bot HTML",
  "scripts": {
    "dev": "http-server . -p 3001 -c-1 --cors",
    "build": "echo 'Add-in build completed'",
    "deploy": "echo 'Deploy to static hosting'",
    "lint": "echo 'No linting needed for addin'",
    "lint:fix": "echo 'No linting needed for addin'",
    "type-check": "echo 'No type checking needed for addin'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
